import styled from 'styled-components';

const Wrapper = styled.div`
  
  .title {
    padding-top: 15px;
  }

  .form-control {
    font-size: 1.4rem;
  }

  .role {
    font-size: 1.4rem;
  }

`;

export default Wrapper;
